# 如何添加其他文章分类

## 概述

MedSys医学知识科普平台支持灵活的文章分类管理。系统预置了一些基本的医学分类，管理员可以通过后台管理系统轻松添加、修改和删除文章分类。

## 预置的文章分类

系统默认包含以下文章分类：

1. **常见疾病** - 普通用户可发布
2. **常见检查** - VIP用户可发布
3. **常用治疗** - VIP用户可发布
4. **常见中医** - 普通用户可发布
5. **常见预防** - 普通用户可发布
6. **常用护理** - VIP用户可发布
7. **药品知识** - 普通用户可发布
8. **健康饮食** - 普通用户可发布
9. **心理健康** - 普通用户可发布
10. **急救常识** - 普通用户可发布

## 如何添加新的文章分类

### 方法一：通过后台管理系统添加（推荐）

1. **登录后台管理系统**
   - 访问系统后台登录页面
   - 使用管理员账号登录

2. **进入文章分类管理**
   - 在左侧导航菜单中找到"文章分类管理"
   - 点击"添加文章分类"

3. **填写分类信息**
   - **类别名称**：输入分类名称（2-20个字符）
     - 示例：运动健康、老年保健、儿童健康等
   - **是否需要VIP特权**：选择该分类是否只允许VIP用户发布文章
     - 选择"否"：所有注册用户都可以在此分类下发布文章
     - 选择"是"：仅VIP用户可以在此分类下发布文章

4. **提交保存**
   - 点击"确定"按钮保存新分类
   - 系统会提示"添加成功"

### 方法二：通过数据库直接添加

如果需要批量添加分类或进行数据库迁移，可以直接在数据库中操作：

1. **连接到MySQL数据库**
   ```bash
   mysql -u root -p
   use medsys;
   ```

2. **插入新的分类记录**
   ```sql
   INSERT INTO `atype` (tname, is_need_vip) VALUES ('分类名称', 0);
   ```
   
   参数说明：
   - `tname`：分类名称
   - `is_need_vip`：0表示普通用户可发布，1表示仅VIP用户可发布

3. **示例**
   ```sql
   -- 添加"运动健康"分类，普通用户可发布
   INSERT INTO `atype` (tname, is_need_vip) VALUES ('运动健康', 0);
   
   -- 添加"专业医学"分类，仅VIP用户可发布
   INSERT INTO `atype` (tname, is_need_vip) VALUES ('专业医学', 1);
   ```

## 管理现有分类

### 修改分类

1. 进入"文章分类管理" → "管理文章分类"
2. 找到要修改的分类，点击编辑按钮（黄色圆形按钮）
3. 修改分类名称或VIP特权设置
4. 点击"确定"保存修改

### 删除分类

1. 进入"文章分类管理" → "管理文章分类"
2. 找到要删除的分类，点击删除按钮（红色圆形按钮）
3. 确认删除操作

**注意**：删除分类前，请确保该分类下没有文章，或已将文章迁移到其他分类。

## 分类命名建议

为了保持系统的专业性和易用性，建议遵循以下命名规范：

1. **简洁明了**：分类名称应该清晰表达分类的主题
2. **字符长度**：2-20个字符（支持中文）
3. **避免重复**：确保新分类名称与现有分类不重复
4. **专业性**：使用医学相关的专业术语
5. **用户友好**：使用用户容易理解的词汇

### 分类名称示例

✅ 推荐的分类名称：
- 妇幼保健
- 老年医学
- 康复医学
- 医学科普
- 健康管理
- 营养保健
- 运动医学
- 睡眠健康
- 口腔保健
- 眼科健康

❌ 不推荐的分类名称：
- 其他（过于笼统）
- a（过于简短）
- 这是一个非常长的分类名称超过了二十个字符（超长）

## 常见问题

### Q1: 为什么有些分类只允许VIP用户发布？

A: VIP特权功能可以用于控制某些专业性强或需要资质认证的内容。例如"常见检查"、"常用治疗"等涉及专业医学知识的分类，建议设置为VIP用户发布，以确保内容的专业性和准确性。

### Q2: 可以添加多少个文章分类？

A: 理论上没有数量限制，但建议控制在20个以内，以保持系统的简洁性和用户体验。

### Q3: 删除分类后，该分类下的文章会怎样？

A: 删除分类前应确保该分类下没有文章。如果有文章，建议先将文章迁移到其他合适的分类，然后再删除空分类。

### Q4: 分类名称可以使用英文吗？

A: 可以，但考虑到目标用户主要是中文用户，建议优先使用中文命名。

## 技术说明

### 数据库表结构

```sql
CREATE TABLE `atype` (
  `tid` int NOT NULL AUTO_INCREMENT,
  `tname` varchar(50) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci NULL DEFAULT '',
  `is_need_vip` int NULL DEFAULT NULL COMMENT '是否是vip才能发布该类文章 0否1是',
  PRIMARY KEY (`tid`) USING BTREE
) ENGINE = InnoDB AUTO_INCREMENT = 11 CHARACTER SET = utf8mb3 COLLATE = utf8mb3_general_ci ROW_FORMAT = DYNAMIC;
```

### 相关API接口

- **查询所有分类**：`GET /back/atype/findAll`
- **分页查询分类**：`GET /back/atype/list?curPage=1&pageSize=10`
- **添加分类**：`POST /back/atype/add`
- **修改分类**：`POST /back/atype/update`
- **删除分类**：`GET /back/atype/del?tid=分类ID`

## 更新日志

- 2026-02-08: 初始版本
  - 完善了文章分类管理功能说明
  - 修复了导航菜单中的文字错误
  - 调整了分类名称验证规则（从4-20字符改为2-20字符）
  - 添加了更多示例分类到数据库
